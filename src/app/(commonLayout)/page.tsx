import { title } from "@/src/components/nextUIComponents/primitives";

export default function HomePage() {
  return (
    <div>
      <h1 className={title({ color: "yellow" })}>Home Page</h1>
    </div>
  );
}
